import 'package:flutter/material.dart';

class HadeethHome extends StatelessWidget {
  const HadeethHome({super.key});

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
